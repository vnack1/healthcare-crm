<IfModule authz_core_module> Require all granted </IfModule>
<IfModule !authz_core_module> Allow from all </IfModule>
<IfModule mod_rewrite.c>
	RewriteEngine On

	# 루트 URL로 접근 시 /login으로 리디렉션
	RewriteCond %{REQUEST_URI} ^/$
	RewriteRule ^(.*)$ /login [L,R=302]
</IfModule>
